# Contributing to Awaj (আওয়াজ) 🚀

Awaj-এ অবদান রাখার জন্য আপনাকে আন্তরিক ধন্যবাদ! এটি একটি উন্মুক্ত ওপেন-সোর্স বাংলা ভয়েস অ্যাসিস্ট্যান্ট প্রকল্প।

## 📌 কীভাবে শুরু করবেন (Getting Started)

1. রিপোজিটরিটি **Fork** করুন।
2. আপনার নিজস্ব ফিচার ব্রাঞ্চ তৈরি করুন:
   ```bash
   git checkout -b feature/new-bangla-command
   ```
3. পরিবর্তন করার পর ইউনিট টেস্ট চালান:
   ```bash
   ./gradlew testDebugUnitTest
   ```
4. পরিবর্তন কমিট করুন:
   ```bash
   git commit -m "feat: add new voice command for weather forecast"
   ```
5. ব্রাঞ্চে পুশ করে একটি **Pull Request** তৈরি করুন।

---

## 🛡️ কোডিং ও সিকিউরিটি নীতিমালা

1. **কোনো হার্ডকোডেড সিক্রেট নয়:** Gemini API Key বা কোনো ক্রেডেনশিয়াল সোর্স কোডে রাখা যাবে না।
2. **আর্থিক নিরাপত্তা গেট:** পেমেন্ট, ওটিপি, পাসওয়ার্ড বা ইউপিআই পিন সংক্রান্ত কোনো অটোমেশন কোড যোগ করা সম্পূর্ণ নিষিদ্ধ।
3. **ইউনিট টেস্ট:** নতুন কোনো ভয়েস কমান্ড যোগ করলে অবশ্যই `RuleParserTest.kt`-এ সংশ্লিষ্ট টেস্ট কেস যুক্ত করতে হবে।
4. **কোড শৈলী:** কোড ক্ল্যাসিটি ও পরিচ্ছন্ন আর্কিটেকচার (Clean Architecture) বজায় রাখুন।

---

## 🐛 বাগ রিপোর্ট বা নতুন ফিচারের অনুরোধ
যেকোনো বাগ বা ফিচারের আইডিয়া জানাতে GitHub Issues-এ নতুন ইস্যু তৈরি করুন।
